import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 32,
    paddingTop: 12,
  },
  button: {
    alignItems: "center",
    marginVertical: 20,
  },
  form: {
    gap: 12,
  },
  inline: {
    flexDirection: "row",
    gap: 12,
  },
  small: {
    width: 74,
  },
});
